* DOM (Document Object Model)
문서를 객체화해서 메모리에 올린 것

* 웹에서 동작 순서
    1. 브라우저에서 페이지 요청
    2. 서버로부터 문서를 받음
    3. 브라우저가 받은 파일을 읽어서 문서의 구조 파악
    4. 문서의 구조를 데이터 구조로 만든다 => DOM
        ( 객체로 처리해서 메모리에 올림 )
    5. 브라우저가 DOm구조를 화면에 출력
        (   이 과정을 'Rendering' )